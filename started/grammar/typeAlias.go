package grammar

// 对于编程而言，类型别名主要用于解决兼容性的问题。

// 类型别名 语法格式：   type 类型别名 = 类型
// 定义类型 语法格式：   type 新的类型名 类型

// 别名类型只会在代码中存在，编译完成时，不会有别名类型。

// 别名类型只会在代码中存在，编译完成时，不会有别名类型。
// • 尽可能地使用 := 去初始化声明一个变量（在函数内部）。
// • 尽可能地使用字符代替字符串。
